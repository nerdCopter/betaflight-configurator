buildscript {
    repositories {
        maven { url "https://repo.grails.org/grails/core/" }
    }
}

allprojects {
    repositories {
        maven { url "https://repo.grails.org/grails/core/" }
    }
}
